version: "2"

sql:
  - engine: "postgresql"
    schema: "./internal/db/schema/schema.sql"
    queries: "./internal/db/queries"
    gen:
      go:
        package: "repo"
        out: "./internal/db/repo"
        sql_package: "pgx/v5"
